<?php

namespace App\Models;

use Illuminate\Contracts\Auth\MustVerifyEmail;
use Illuminate\Foundation\Auth\User as Authenticatable;
use Illuminate\Notifications\Notifiable;
use Illuminate\Support\Facades\Hash;

// class Admin extends Authenticatable {
class Admin extends User {
    use Notifiable;


    protected $fillable = [
        'name', 'email', 'password', 'retired', 'date_of_retired',
    ];

    protected $hidden = [
        'password', 'remember_token',
    ];

    protected $casts = [
        'email_verified_at' => 'datetime',
    ];
    
    //　ユーザを検索する
    //
    public static function search( $find, $sort = null, $asc_desc = null ) {
        
        // dump( $find );
        if( isset( $find['retired'] )) {
            if( $find['retired'] != 'all' ) {
                $admins = Admin::where( 'retired', $find['retired'] );
            } else {
                $admins = Admin::select( "*" );
            }
        } else {
            $admins = Admin::select( "*" );
        }

        if( isset( $find['name'])) {
            $admins = $admins->where( 'name', 'like', '%'.$find['name'].'%' );
        }
        
        if( isset( $find['email'] )) {
            $admins = $admins->where( 'email', 'like', '%'.$find['email'].'%' ); 
        }

        //  ソート
        //
        if( isset( $sort ) && is_array( $sort )) {
            foreach( $sort as $i => $s ) {
                if( empty( $s )) { continue; }
                if( ! empty( $asc_desc[$i]) ) {
                    $admins = $admins->orderBy( $s, $asc_desc[$i] );
                } else {
                    $admins = $admins->orderBy( $s );
                }
            }
        }
        // dump( $admins );
                
        if( isset( $find['paginate'] )) {
            return $admins->paginate( $find['paginate'] );
        } else {
            return $admins->get();
        }
    }
    
    //  管理者かユーザか顧客か
    //
    public function is_admin() {
        return true;
    }      // true
    public function is_user() {
        return false;
    }       // false
    public function is_customer() {
        return false;
    }   // false
    
    //　退職済みかどうか
    //
    public function is_retired() {
        if( $this->retired ) { return true; } else { return fales; }
    }
    public function is_in_office() {
        return ! $this->is_retired();
    }
    

    
}
